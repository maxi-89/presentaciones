---
name: "Commit"
description: Stage, commit, and open a PR for current or scoped changes
category: Workflow
---

Read the file `openspec/.commands/commit.md` and follow all its instructions exactly, using `$ARGUMENTS` as the arguments.
